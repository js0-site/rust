1.

修改测试。把 insert_if_not_contained 改为 insert
test_and_add 改为 add
add_if_not_exist 改为 add

并在数据准备阶段

确保所有插入的数据
都是唯一不重复的

2. 更新文档，把 安全使用指南
先添加一节布谷鸟的原理，为下面如何避免误用添加解读
的 删除操作与假阴性 作为 第二节
添加 如何正确的避免重复插入
如果反复插入同一个 key ， 会导致布谷鸟过滤器爆掉
如果用布谷鸟过滤器，检查是否存在，再插入，因为假阳性，导致2个 hash 相同的 key 只插入一次，进而导致删除的时候出现加假阴性
正确的流程是，用数据库检查是否存在，不存在再插入