优化尝试 #6：使用 ZERO 常量替代 default() 调用
==============================================

日期：2026-01-16
状态：✅ 保留（无性能变化，但代码更清晰）

## 修改内容
1. 在 Fingerprint trait 中添加 const ZERO: Self
2. 在 impl_fingerprint 宏中实现 const ZERO: Self = 0
3. 将 contains_impl 中的 T::default() 替换为 T::ZERO

## 理论依据
显式使用常量 ZERO 比调用 default() 语义更清晰，
虽然编译器应该已经将 default() 优化为常量。

## 性能结果

| 过滤器 | 查询性能变化 |
|--------|--------------|
| BinaryFuse8 | +1.16% |
| BinaryFuse16 | -0.86% |
| BinaryFuse32 | -0.51% |

变化在 ±1.5% 范围内，属于测量噪声。

## 结论
性能无明显变化，但代码语义更清晰，保留此改动。
证实编译器已经将 default() 优化为常量 0。
