优化尝试 #3：复用 h1_base 中间计算结果
========================================

日期：2026-01-16
状态：❌ 失败（回滚）

## 修改内容
尝试复用 h0 + seg_len 的中间结果：
  let h1_base = h0.wrapping_add(seg_len);
  let h1 = h1_base ^ ...
  let h2 = h1_base.wrapping_add(seg_len) ^ ...

## 理论依据
减少一次加法操作，通过复用中间计算结果。

## 性能结果

| 过滤器 | 查询性能变化 |
|--------|--------------|
| BinaryFuse8 | +0.69% |
| BinaryFuse16 | -0.57% |
| BinaryFuse32 | -0.77% |

## 结论
结果在噪声范围内波动，整体没有明显提升。
编译器可能已经做了类似的优化，或者这个改动打乱了编译器的优化策略。
决定回滚。
